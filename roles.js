'use strict';
let unit_builder = require('unit_builder');
let unit_soldier = require('unit_soldier');
let unit_harvester = require('unit_harvester');

module.exports = {
	HARVESTER: unit_harvester,
	BUILDER: unit_builder,
	SOLDIER: unit_soldier
}